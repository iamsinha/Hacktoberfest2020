#include <stdio.h>

int main()
{
  int a, b;
  int c = 1;
  printf ("Enter the number:");
  scanf("%d", &b);
  if (b<0)
  printf ("error.. Enter the positive number");
  else 
  {
     for(a=1; a<=b; a++)
      c = c*a;
  }
  printf ("The factorial of %d is: %d", b,c);
  
  return 0;
}
